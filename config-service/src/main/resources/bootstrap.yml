server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GpeMix/microservices-curso.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"
  security:
    user:
      username: root
      password: s3cr3t